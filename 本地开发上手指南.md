# 本地开发上手指南

> 本文档帮助你在本地快速接续开发。项目代码和方案文档已全部在仓库中。

---

## 一、环境准备

### Python 环境

```bash
# 克隆仓库
git clone https://github.com/Dawn-1122/Newlife.git
cd Newlife

# 建议用虚拟环境
python3 -m venv venv
source venv/bin/activate    # Mac/Linux
# venv\Scripts\activate     # Windows

# 安装依赖
pip install -r requirements.txt

# 生成数据文件（字库+诗词库）
python3 scripts/generate_char_db.py
python3 scripts/generate_poetry_db.py
```

### 微信开发者工具

1. 下载 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 用微信扫码登录
3. 导入项目 → 目录选择 `Newlife/miniprogram/`
4. AppID 填你自己的（或选"测试号"）

---

## 二、启动开发

### 后端

```bash
# 在项目根目录
python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

- API文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/api/v1/health

### 小程序

1. 打开微信开发者工具，加载 `miniprogram/` 目录
2. 打开 `miniprogram/app.js`，确认 `apiBase` 指向你的后端地址：
   ```js
   apiBase: 'http://localhost:8000/api/v1'
   ```
3. 微信开发者工具中：详情 → 本地设置 → 勾选「不校验合法域名」（开发阶段必须勾，否则localhost请求会被拦截）

---

## 三、项目结构速览

```
Newlife/
├── 综合起名平台方案建议.md   ← 完整产品方案（市场/功能/架构/变现/路线图）
├── README.md                ← 项目说明
├── 本地开发上手指南.md       ← 你正在看的这份
├── requirements.txt         ← Python依赖
│
├── app/                     ← 后端代码
│   ├── main.py              ← FastAPI入口
│   ├── api/routes.py        ← API路由（6个接口）
│   ├── core/
│   │   ├── config.py        ← 配置
│   │   └── constants.py     ← 天干地支/五行常量
│   ├── schemas/schemas.py   ← 请求/响应数据模型
│   └── services/
│       ├── bazi_engine.py   ← 八字排盘引擎（核心）
│       ├── char_database.py ← 字库服务
│       ├── poetry_database.py ← 诗词库服务
│       ├── phonetics.py     ← 音律评分
│       ├── wuge.py          ← 五格数理
│       ├── naming_engine.py ← 起名核心引擎（整合以上所有）
│       └── llm_service.py   ← LLM寓意解读
│
├── data/                    ← 数据文件（由脚本生成）
│   ├── dict/chars.json      ← 字库（148字）
│   └── poetry/poetry.json   ← 诗词库（42条）
│
├── miniprogram/             ← 微信小程序
│   ├── app.js / app.json / app.wxss
│   ├── pages/
│   │   ├── index/           ← 起名输入页
│   │   ├── result/          ← 结果列表页
│   │   └── detail/          ← 名字详情页
│   └── utils/api.js         ← API调用封装
│
├── scripts/
│   ├── generate_char_db.py  ← 字库生成脚本
│   ├── generate_poetry_db.py ← 诗词库生成脚本
│   └── llm_benchmark.py     ← LLM对比测试脚本
│
└── tests/
    └── test_core.py         ← 核心引擎测试
```

---

## 四、核心API接口

| 接口 | 方法 | URL | 说明 |
|------|------|-----|------|
| 健康检查 | GET | `/api/v1/health` | 验证服务是否正常 |
| 生成名字 | POST | `/api/v1/generate` | 传入姓氏/性别/生辰，返回候选名字列表 |
| 解析名字 | POST | `/api/v1/analyze` | 传入完整姓名，返回五行/音律/五格分析 |
| 查询字库 | GET | `/api/v1/chars` | 按五行/笔画/性别筛选汉字 |
| 查询诗词 | GET | `/api/v1/poetry` | 按字/意象/来源筛选诗词 |
| 八字排盘 | GET | `/api/v1/bazi` | 传入出生时间，返回八字分析 |

### 起名接口示例

```bash
curl -X POST http://localhost:8000/api/v1/generate \
  -H "Content-Type: application/json" \
  -d '{
    "surname": "王",
    "gender": "male",
    "year": 2025,
    "month": 3,
    "day": 15,
    "hour": 10,
    "name_length": 2,
    "max_results": 10
  }'
```

---

## 五、运行测试

```bash
python3 tests/test_core.py
```

会依次测试：八字排盘 → 字库 → 诗词库 → 音律评分 → 五格数理 → 完整起名流程。

---

## 六、LLM对比测试

### 1. 申请API Key

| 提供商 | 注册地址 | 模型 | 特点 |
|--------|----------|------|------|
| DeepSeek | platform.deepseek.com | deepseek-chat | 便宜，中文能力强 |
| 通义千问 | dashscope.aliyun.com | qwen-plus | 阿里出品，生态好 |
| 智谱 | open.bigmodel.cn | glm-4 | 清华背景，学术强 |

### 2. 设置环境变量

```bash
export DEEPSEEK_API_KEY=sk-xxx
export QWEN_API_KEY=sk-xxx
export ZHIPU_API_KEY=sk-xxx
```

### 3. 运行测试

```bash
python3 scripts/llm_benchmark.py
```

测试报告输出到 `output/llm_benchmark_report.json`。

### 4. 选定后配置

在 `app/core/config.py` 中填入选定的LLM：
```python
LLM_PROVIDER: str = "deepseek"  # 你选的
LLM_API_KEY: str = "sk-xxx"
LLM_MODEL: str = "deepseek-chat"
```

---

## 七、待优化清单（按优先级）

### P0 — 影响核心体验

- [ ] **名字组合算法优化**：当前会出现"张清水"这类两字堆叠的搭配，需加搭配规则（避免同义字、同部首、同五行堆叠）
- [ ] **五格数理笔画校验**：部分字的康熙笔画可能有误，需对照康熙字典逐字校验
- [ ] **字库扩充**：当前148字，至少扩充到2000+常用起名字

### P1 — 提升产品品质

- [ ] **诗词库扩充**：当前42条，目标500+条（诗经/楚辞/唐诗/宋词/四书五经）
- [ ] **名字故事卡**：生成可分享的精美图片（含诗词出处+五行图+评分）
- [ ] **八字节气精确化**：当前立春/节气用近似日期，需查精确时刻表

### P2 — 功能扩展

- [ ] **品牌起名模块**（含商标/域名查重）
- [ ] **网名/笔名生成**
- [ ] **付费体系**（微信支付接入）
- [ ] **用户系统**（收藏/历史记录）

---

## 八、Git协作流程

### 云端改了代码 → 本地同步

```bash
git pull origin main
```

### 本地改了代码 → 推到云端

```bash
git add -A
git commit -m "描述你改了什么"
git push origin main
```

> 注意：不要同时在云端和本地改代码再push，容易冲突。养成push前先pull的习惯。

---

## 九、常见问题

**Q: 后端启动报 `ModuleNotFoundError`？**
A: 确认在项目根目录执行，且已激活虚拟环境并安装了requirements.txt。

**Q: 小程序请求后端报错？**
A: 检查三件事：①后端是否在运行 ②app.js中apiBase是否正确 ③微信开发者工具是否勾了"不校验合法域名"。

**Q: 生成数据文件报错？**
A: 确保先执行 `generate_char_db.py` 再执行 `generate_poetry_db.py`，它们会写入 `data/` 目录。

**Q: 测试脚本报 `No module named 'app'`？**
A: 在项目根目录执行 `PYTHONPATH=. python3 tests/test_core.py`，或直接 `python3 tests/test_core.py`（脚本已内置path处理）。
